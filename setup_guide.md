# セットアップガイド

初回設定の手順です。1つずつ順番に進めてください。

---

## Step 1: GitHubアカウントを作成する

既にGitHubアカウントをお持ちの場合はStep 2に進んでください。

1. https://github.com にアクセス
2. 「Sign up」ボタンをクリック
3. メールアドレス、パスワード、ユーザー名を入力
4. 無料プラン（Free）を選択
5. メールに届く確認コードを入力して完了

---

## Step 2: 新しいリポジトリを作成する

「リポジトリ」とは、GitHubの上にあるフォルダのようなものです。ここにファイルを置いて管理します。

1. GitHubにログインした状態で https://github.com/new にアクセス
2. 以下のように入力:
   - Repository name: `ai-news-digest`（好きな名前でOK）
   - Description: `AIニュースまとめ`（任意）
   - Public を選択（GitHub Pagesを無料で使うために必要）
   - 「Add a README file」にチェックを入れる
3. 「Create repository」ボタンをクリック

---

## Step 3: ファイルをアップロードする

作成したリポジトリにファイルをアップロードします。

1. 作成したリポジトリのページを開く
2. 「Add file」→「Upload files」をクリック
3. エクスプローラーで以下のフォルダをすべて選択してドラッグ＆ドロップ:
   - `.github/` フォルダ
   - `scripts/` フォルダ
   - `templates/` フォルダ
   - `data/` フォルダ
   - `docs/` フォルダ
   - `README.md` ファイル

注意: GitHubのWebからはフォルダ構造をそのままアップロードできない場合があります。
その場合は、以下の方法で1ファイルずつ作成してください。

### フォルダ構造を1ファイルずつ作成する方法

1. リポジトリのページで「Add file」→「Create new file」をクリック
2. ファイル名の欄に `scripts/sources.yml` と入力（`/`を入力するとフォルダが自動作成される）
3. ファイルの内容をコピー＆ペースト
4. 「Commit changes」をクリック
5. 同じ手順を以下のファイルすべてに対して繰り返す:
   - `scripts/fetch_news.py`
   - `scripts/requirements.txt`
   - `templates/template.html`
   - `data/archive.json`
   - `.github/workflows/daily_news.yml`

---

## Step 4: GitHub Pages を有効にする

GitHub Pages は、リポジトリ内のHTMLファイルをWebページとして公開する無料サービスです。

1. リポジトリのページで「Settings」タブをクリック
2. 左メニューの「Pages」をクリック
3. 「Source」セクションで:
   - Branch: `main` を選択
   - フォルダ: `/docs` を選択
4. 「Save」をクリック
5. 数分待つと、ページ上部に公開URLが表示される
   - URL形式: `https://あなたのユーザー名.github.io/ai-news-digest/`

---

## Step 5: 初回テスト実行

自動更新が正しく動くかテストします。

1. リポジトリのページで「Actions」タブをクリック
2. 左メニューから「AIニュースまとめ 自動更新」をクリック
3. 「Run workflow」ボタンをクリック
4. 「Run workflow」（緑色ボタン）を再度クリック
5. 実行が始まる（2〜3分かかります）
6. 緑のチェックマーク ✅ が表示されたら成功

成功したら、Step 4 で確認したURLにアクセスしてみてください。
AIニュースの一覧が表示されるはずです。

---

## Step 6: スマホのホーム画面にショートカットを追加する

毎日ワンタップでアクセスできるようにします。

### iPhoneの場合
1. Safariで公開URLを開く
2. 画面下の共有ボタン（□に↑のアイコン）をタップ
3. 「ホーム画面に追加」をタップ
4. 名前を「AIニュース」など好きな名前に変更
5. 「追加」をタップ

### Androidの場合
1. Chromeで公開URLを開く
2. 右上の「⋮」メニューをタップ
3. 「ホーム画面に追加」をタップ
4. 名前を「AIニュース」など好きな名前に変更
5. 「追加」をタップ

---

## 日常の使い方

設定完了後は何もする必要はありません。

- 毎朝7:00に自動で最新ニュースが更新される
- 通勤中にスマホのホーム画面からアイコンをタップして読む
- カテゴリフィルターで読みたいジャンルを絞り込める
- 読んだ記事は「既読」ボタンで半透明になる
- 気になった記事は「元記事を読む」で元のサイトに飛べる

---

## ニュースソースを追加・変更したいとき

1. GitHubのリポジトリページを開く
2. `scripts/sources.yml` ファイルをクリック
3. 鉛筆アイコン（✏️）をクリックして編集
4. ソースを追加・変更・削除（enabled: false で無効化）
5. 「Commit changes」をクリック
6. 次回の自動実行から反映される（すぐ反映したい場合は手動実行）

---

## トラブルシューティング

### ページが更新されない
- 「Actions」タブで最新の実行結果を確認してください
- 赤い ❌ が表示されている場合はエラーが発生しています
- エラーの詳細をクリックして、どのステップで失敗したか確認してください

### 記事が少ない・表示されない
- RSSフィードのURLが変更された可能性があります
- `sources.yml` のURLを確認し、ブラウザで直接アクセスしてRSSが取得できるか確認してください
- 取得できないソースは `enabled: false` に変更してください

### ページのデザインが崩れる
- ブラウザのキャッシュをクリアしてリロードしてください
- iPhone: Safari設定 → 「履歴とWebサイトデータを消去」
- Android: Chrome設定 → 「閲覧履歴データの削除」
